유저 소셜 로그인을 통한 토큰 생성
---
tags:
  - name: "[dev] User Create Token"
parameters:
  - in: body
    name: category
    type: string
    enum: [kakao, apple]
    required: true
    description: social login provider 종류
  - in: body
    name: access_token
    type: string
    required: true
    description: social login provider 가 제공한 token
responses:
  200:
    description: TBD
    schema:
      $ref: "#/definitions/Palette"
    examples:
      rgb: ["red", "green", "blue"]
